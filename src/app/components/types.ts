import { SvgIconComponent } from "@mui/icons-material";

export type NavInfo = {
    icon: SvgIconComponent;
    localeStr: string;
    path: string;
}

export type GithubUserInfo = {
    avatar_url: string;
}